services:
  - type: web
    name: synthetica-demo
    env: python
    buildCommand: pip install -r requirements-render.txt
    startCommand: python -m uvicorn app.simple_demo:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
